{{! /event/:id/update }}
<div class="container-fluid">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h1 class="event-create">Update Event</h1>
      <form class="event_create" action="/event/:update" method="POST">
        <ul>
          <li>
            <label for="title">Title</label>
            <input type="text" name="title" value="{{data.title}}" require>
          </li>
          <li>
            <label for="location">Location/building</label>
            <input type="text" name="location" value="{{data.location}}">
          </li>
          <li>
            <label for="street">Street</label>
            <input type="text" name="street" value="{{data.street}}" require>
          </li>
          <li>
            <label for="city">City</label>
            <input type="text" name="city" value="{{data.city}}" require>
          </li>
          <li>
            <label for="start_date">Start Date</label>
            <input type="date" name="start_date" value="{{data.start_date}}" require>
          </li>
          <li>
            <label for="start_time">Start Time</label>
            <input type="time" name="start_time" value="{{data.start_time}}" require>
          </li>
          <li>
            <label for="end_date">End Date</label>
            <input type="date" name="end_date" value="{{data.end_date}}" require>
          </li>
          <li>
            <label for="end_time">End Time</label>
            <input type="time" name="end_time" value="{{data.end_time}}" require>
          </li>
          <li>
            <label for="">Start Date</label>
            <input type="date" name="start_date"  value="{{data.start_date}}" require>
          </li>
          <li>
            <label for="available_positions">Number of Volunteers Desired</label>
            <input type="number" name="available_positions" value="{{data.available_positions}}">
          </li>
          <li>
            <label for="description">Event Description</label>
            <textarea rows='8' name="description" require>{{data.description}}</textarea>
          </li>
          <li>
            <input type="submit" class="submit-btn" name="loginVolSubmit" value="Submit" require>
          </li>
        </ul>
      </form>
    </div>
  </div>
</div>
